openapi: 3.2.0
info:
  title: Bioflyte Organization API
  version: '1.0'
  description: 'Operations tagged Organization across 2 of this provider''s published API definitions: bioflyte-organization-api-openapi.yml, bioflyte-portal-openapi-original.json. Each path carries the servers of the definition it was published in.'
servers:
- url: https://portal.bioflyte.com
  description: Base URL declared by the provider in apis.yml (roadmap#122).
tags:
- name: Organization
paths:
  /LoadLocationsByOrgId:
    post:
      tags:
      - Organization
      requestBody:
        content:
          application/json-patch+json:
            schema:
              $ref: '#/components/schemas/ObjId'
          application/json:
            schema:
              $ref: '#/components/schemas/ObjId'
          text/json:
            schema:
              $ref: '#/components/schemas/ObjId'
          application/*+json:
            schema:
              $ref: '#/components/schemas/ObjId'
      responses:
        '200':
          description: OK
    servers:
    - url: https://portal.bioflyte.com
      description: Base URL declared by the provider in apis.yml (roadmap#122).
components:
  schemas:
    ObjId:
      type: object
      properties:
        id:
          type: string
          format: uuid
      additionalProperties: false
x-refined-from:
- bioflyte-organization-api-openapi.yml
- bioflyte-portal-openapi-original.json
